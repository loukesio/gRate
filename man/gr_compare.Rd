% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{gr_compare}
\alias{gr_compare}
\title{Compare growth parameters between strains or conditions}
\usage{
gr_compare(
  plate,
  what = c("r", "K", "lag", "doubling_time", "N0", "t_rmax"),
  by = NULL,
  method = c("welch", "kruskal"),
  drop_flagged = TRUE,
  conf_level = 0.95
)
}
\arguments{
\item{plate}{A \code{gr_plate} object after \code{\link[=gr_fit]{gr_fit()}} (layout with metadata
required; \code{\link[=gr_qc]{gr_qc()}} recommended).}

\item{what}{Which parameter to compare: \code{"r"} (default), \code{"K"}, \code{"lag"},
\code{"doubling_time"}, \code{"N0"}, or \code{"t_rmax"}.}

\item{by}{Character vector of metadata columns defining the groups.
Default: every metadata column except \code{bio_rep} and \code{tech_rep}.}

\item{method}{\code{"welch"} (default) or \code{"kruskal"} (see Details).}

\item{drop_flagged}{If \code{TRUE} (default), wells flagged by \code{\link[=gr_qc]{gr_qc()}} are
excluded before averaging and testing.}

\item{conf_level}{Confidence level for the per-group intervals shown in
\verb{$groups} and by \code{\link[=gr_plot_compare]{gr_plot_compare()}}. Default \code{0.95}.}
}
\value{
An object of class \code{gr_compare}: a list with
\itemize{
\item \verb{$groups} — per-group tibble: n (replicates), mean, sd, se, ci_lo,
ci_hi;
\item \verb{$overall} — the overall test (method, statistic, p_value);
\item \verb{$pairwise} — tibble of pairwise comparisons with adjusted p-values;
\item \verb{$data} — the replicate-level values the tests ran on;
\item \verb{$what}, \verb{$by}, \verb{$method}, \verb{$unit} — what was compared and how.
}
}
\description{
Tests whether a growth parameter (growth rate \code{r} by default) differs
between groups — strains, media, treatments — doing the statistics at the
right level of replication. Technical replicates are averaged into their
biological replicate \strong{before} any test, so wells never inflate the sample
size (the pseudoreplication mistake). Flagged wells and failed fits are
excluded first.
}
\details{
The unit of replication is chosen automatically: if the layout designates
\code{bio_rep}, each biological replicate contributes one value per group
(its technical replicates averaged); without \code{bio_rep}, each well is
treated as a replicate — fine only if your wells really are independent
cultures.

With \code{method = "welch"} (default) the overall test is Welch's t-test for
two groups or Welch's ANOVA (\code{\link[stats:oneway.test]{stats::oneway.test()}}) for more, followed by
pairwise Welch t-tests with Holm adjustment. \code{method = "kruskal"} uses the
Kruskal-Wallis test and pairwise Wilcoxon tests instead — for small or
clearly non-normal samples.
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate")) |>
  gr_layout(system.file("extdata", "layout_long.csv", package = "gRate")) |>
  gr_qc() |>
  gr_fit()

cmp <- gr_compare(plate, what = "r", by = "strain")
cmp
cmp$pairwise
}
\seealso{
\code{\link[=gr_plot_compare]{gr_plot_compare()}} to visualise, \code{\link[=gr_fit_summary]{gr_fit_summary()}} for plain
descriptive summaries.
}
