% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gr_plate.R
\name{new_gr_plate}
\alias{new_gr_plate}
\title{Construct a \code{gr_plate} object}
\usage{
new_gr_plate(data, qc = NULL, meta = list())
}
\arguments{
\item{data}{A data frame with columns \code{well}, \code{time}, \code{value}. \code{row} and
\code{col} are derived from \code{well} if absent. Well ids are normalised (\code{"A01"}
becomes \code{"A1"}).}

\item{qc}{Optional per-well QC tibble (normally left \code{NULL}; \code{\link[=gr_qc]{gr_qc()}}
fills it in).}

\item{meta}{Named list of plate-level metadata.}
}
\value{
A \code{gr_plate} object.
}
\description{
\code{gr_plate} is the single data structure every gRate function takes and
returns. It is a list with three components:
}
\details{
\itemize{
\item \verb{$data} — a tidy tibble with one row per well and timepoint, columns
\code{well}, \code{row}, \code{col}, \code{time}, \code{value}, plus any metadata columns added by
\code{\link[=gr_layout]{gr_layout()}} and a \code{value_raw} column after \code{\link[=gr_spatial]{gr_spatial()}} correction.
\item \verb{$qc} — a per-well tibble of QC flags, filled in by \code{\link[=gr_qc]{gr_qc()}}
(\code{NULL} before QC has run).
\item \verb{$meta} — a list of plate-level metadata: \code{instrument}, \code{plate_id},
\code{read_interval}, and bookkeeping added by other functions.
}

You rarely need to call \code{new_gr_plate()} yourself — \code{\link[=gr_read]{gr_read()}} builds the
object from a raw export. It is exported so you can construct a plate from
data that is already tidy.
}
\examples{
df <- expand.grid(well = c("A1", "A2"), time = 0:5)
df$value <- 0.05 + 0.01 * df$time
plate <- new_gr_plate(df)
plate
}
