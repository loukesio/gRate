% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_plate.R
\name{gr_read}
\alias{gr_read}
\title{Read a plate reader export into a \code{gr_plate} object}
\usage{
gr_read(
  x,
  format = c("auto", "wide", "long", "tecan", "biotek"),
  time_col = NULL,
  plate_id = NULL,
  ...
)
}
\arguments{
\item{x}{Path to a CSV/TSV/Excel file, or a data frame that is already in
wide or long layout.}

\item{format}{One of \code{"auto"}, \code{"wide"}, \code{"long"}. (\code{"tecan"} and
\code{"biotek"} are reserved for upcoming instrument parsers.)}

\item{time_col}{For wide data: name of the time column. Defaults to a column
named like time (\code{time}, \code{hours}, \code{hour}, \code{t}), or the first column.}

\item{plate_id}{Optional plate identifier stored in \verb{$meta}. Defaults to the
file name (without extension) when \code{x} is a path.}

\item{...}{Additional arguments passed to \code{\link[utils:read.table]{utils::read.csv()}} or
\code{\link[readxl:read_excel]{readxl::read_excel()}} when \code{x} is a path.}
}
\value{
A \link[=new_gr_plate]{gr_plate} object.
}
\description{
Parses raw growth curve data into the tidy \code{gr_plate} structure that every
other gRate function operates on. Two generic layouts are supported:
}
\details{
\itemize{
\item \strong{wide} — one row per timepoint: a time column plus one column per well
(\code{A1}, \code{A01}, \code{B12}, ...). This is the layout most plate reader software
exports to CSV.
\item \strong{long} — one row per well and timepoint, with columns for well, time,
and measurement. Column names are matched case-insensitively (well:
\code{well}; time: \code{time}, \code{hours}, \code{hour}, \code{t}; value: \code{value}, \code{od}, \code{od600},
\code{absorbance}, \code{measurement}, \code{abs}).
}

With \code{format = "auto"} (the default) the layout is detected from the column
names. Instrument-specific parsers for Tecan and BioTek exports are planned;
\code{format = "tecan"} and \code{format = "biotek"} currently stop with an
informative error because those parsers are only written against real
example exports, not guessed formats.
}
\examples{
# long format
path <- system.file("extdata", "growth_long.csv", package = "gRate")
gr_read(path)

# wide format
path <- system.file("extdata", "growth_wide.csv", package = "gRate")
gr_read(path, format = "wide")
}
\seealso{
\code{\link[=gr_layout]{gr_layout()}} to attach well metadata, \code{\link[=gr_qc]{gr_qc()}} to flag wells.
}
