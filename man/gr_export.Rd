% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{gr_export}
\alias{gr_export}
\title{Export a \code{gr_plate} for downstream analysis}
\usage{
gr_export(
  plate,
  as = c("tidy", "growthcurver", "gcplyr"),
  drop_flagged = FALSE,
  collapse_tech = FALSE
)
}
\arguments{
\item{plate}{A \code{gr_plate} object.}

\item{as}{Output shape: \code{"tidy"}, \code{"growthcurver"}, or \code{"gcplyr"}.}

\item{drop_flagged}{If \code{TRUE}, wells flagged by \code{\link[=gr_qc]{gr_qc()}} are removed from
the export. Default \code{FALSE} — gRate flags, you decide. Requires
\code{\link[=gr_qc]{gr_qc()}} to have run.}

\item{collapse_tech}{If \code{TRUE}, technical replicates are averaged: rows are
grouped by every metadata column except the well identity and \code{tech_rep},
plus time, and \code{value} becomes their mean. Requires a \code{tech_rep} column
(designate one with \code{\link[=gr_layout]{gr_layout()}}) and only makes sense for the tidy
shape, since averaged curves no longer belong to a single well. The
result gains \code{n_wells} (wells averaged) and, if QC has run, \code{flagged}
is \code{TRUE} when \emph{any} contributing well was flagged (combine with
\code{drop_flagged = TRUE} to average only clean wells).}
}
\value{
A data frame in the requested shape.
}
\description{
Turns a processed plate back into a plain data frame, in the shape a
downstream tool expects:
}
\details{
\itemize{
\item \code{"tidy"} (default) — one row per well and timepoint, with all metadata
columns and, when \code{\link[=gr_qc]{gr_qc()}} has run, \code{flagged} and \code{reasons} joined in.
\item \code{"growthcurver"} — a wide data frame with a \code{time} column and one column
per well, ready for \code{growthcurver::SummarizeGrowthByPlate()}.
\item \code{"gcplyr"} — a long data frame with columns \code{Well}, \code{Time},
\code{Measurements} (plus metadata), the tidy shape gcplyr's design functions
expect.
}
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
plate <- gr_qc(plate)

head(gr_export(plate))
gc_input <- gr_export(plate, as = "growthcurver", drop_flagged = TRUE)
names(gc_input)[1:5]
}
\seealso{
\code{\link[=as_growthcurver]{as_growthcurver()}}, \code{\link[=as_gcplyr]{as_gcplyr()}}
}
