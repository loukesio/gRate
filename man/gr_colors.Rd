% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\docType{data}
\name{gr_colors}
\alias{gr_colors}
\alias{theme_gr}
\title{gRate colors and ggplot2 theme}
\format{
\code{gr_colors} is a named list of hex colors and character vectors.
}
\usage{
gr_colors

theme_gr(base_size = 11)
}
\arguments{
\item{base_size}{Base font size in points. Default \code{11}.}
}
\value{
\code{theme_gr()} returns a ggplot2 theme object; add it to any plot.
}
\description{
All gRate plots share one design system: a colorblind-validated categorical
palette (assigned in fixed slot order, never cycled), a single-hue blue ramp
for magnitude (heatmaps), a reserved status red for flagged wells, and a
quiet chart chrome — hairline gridlines, muted axis ink, no decoration
louder than the data.
}
\details{
\code{gr_colors} exposes the named roles so your own plots can match:
\verb{$series} (eight categorical hues in slot order), \verb{$sequential} (a
light-to-dark blue ramp), \verb{$flagged} (status red), \verb{$fitted} (the fit-line
blue), and the ink roles \verb{$ink}, \verb{$ink2}, \verb{$muted}, \verb{$grid}, \verb{$baseline},
\verb{$surface}, \verb{$neutral}.
}
\examples{
gr_colors$series[1:3]
gr_colors$flagged
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) +
  geom_point(colour = gr_colors$series[1]) +
  theme_gr()
}
\keyword{datasets}
