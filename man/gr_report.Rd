% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{gr_report}
\alias{gr_report}
\title{Render a one-page HTML QC report for a plate}
\usage{
gr_report(
  plate,
  file = "gRate_report.html",
  title = NULL,
  interactive = FALSE,
  quiet = TRUE
)
}
\arguments{
\item{plate}{A \code{gr_plate} object, ideally after \code{\link[=gr_qc]{gr_qc()}} (and optionally
\code{\link[=gr_spatial]{gr_spatial()}}).}

\item{file}{Output HTML file path. Default \code{"gRate_report.html"} in the
working directory.}

\item{title}{Report title. Defaults to the plate id if present.}

\item{interactive}{If \code{TRUE}, plots are rendered as zoomable, hoverable
plotly widgets (hover a curve to see its well) and the results table
becomes searchable and sortable. Requires the \code{plotly} and \code{DT}
packages. Default \code{FALSE} (static figures).}

\item{quiet}{Passed to \code{\link[quarto:quarto_render]{quarto::quarto_render()}}. Default \code{TRUE}.}
}
\value{
The path to the rendered HTML file, invisibly.
}
\description{
Renders the bundled Quarto (\code{.qmd}) template into a standalone,
self-contained HTML report summarising the plate: metadata, per-well QC
flags with the thresholds used, plate heatmaps, growth curves, and (if run)
the spatial correction factors. Requires the \code{quarto} R package and the
Quarto CLI (bundled with recent RStudio, or from
\url{https://quarto.org/docs/get-started/}).
}
\examples{
\donttest{
if (requireNamespace("quarto", quietly = TRUE) &&
    !is.null(quarto::quarto_path())) {
  plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
  plate <- gr_qc(plate)
  out <- gr_report(plate, file = file.path(tempdir(), "report.html"))
}
}
}
