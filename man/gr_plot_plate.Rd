% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{gr_plot_plate}
\alias{gr_plot_plate}
\title{Plate heatmap of a statistic, QC flag, or metadata variable}
\usage{
gr_plot_plate(
  plate,
  fill = "max_od",
  label = FALSE,
  equalize = FALSE,
  palette = NULL
)
}
\arguments{
\item{plate}{A \code{gr_plate} object.}

\item{fill}{What to colour wells by (see Details). Default \code{"max_od"}.}

\item{label}{If \code{TRUE}, print the value in each well (rounded for numeric
fills). Default \code{FALSE}.}

\item{equalize}{For numeric fills: if \code{TRUE}, the ramp's colours are
anchored at the data's own quantiles, so wells clustered in a narrow
value band separate visibly. \strong{Off by default}: equalization also
exaggerates trivial differences, making a quiet plate look dramatic.
The default linear mapping on the perceptually uniform ramp shows equal
differences equally.}

\item{palette}{For numeric fills: \code{NULL} (default) uses the perceptually
uniform rocket ramp; a vector of colours is used as a custom ramp; a
single string is looked up as a palette name from the
\href{https://github.com/loukesio/ltc_palettes}{ltc package} (e.g.
\code{"heatmap0"}, \code{"maya"}), which must be installed. Note that most named
palettes are not perceptually uniform — the default is the safe choice.}
}
\value{
A ggplot object (modify or print it like any other ggplot).
}
\description{
Draws the plate as an 8 x 12 grid (row A at the top, as you look at the
plate) with each well coloured by:
}
\details{
\itemize{
\item a summary statistic — \code{"max_od"} (default), \code{"auc"}, \code{"delta_od"},
\code{"baseline"};
\item a QC result — \code{"flagged"} or any individual check name run by \code{\link[=gr_qc]{gr_qc()}}
(e.g. \code{"spike"});
\item a growth parameter from \code{\link[=gr_fit]{gr_fit()}} — \code{"r"}, \code{"K"}, \code{"lag"},
\code{"doubling_time"}, \code{"t_rmax"}, \code{"N0"}, \code{"sigma"}, or \code{"fit_ok"};
\item any metadata column added by \code{\link[=gr_layout]{gr_layout()}}.
}
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
plate <- gr_qc(plate)
gr_plot_plate(plate, "max_od")
gr_plot_plate(plate, "flagged")
}
