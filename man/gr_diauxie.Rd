% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diauxie.R
\name{gr_diauxie}
\alias{gr_diauxie}
\title{Detect multi-phase (diauxic) growth}
\usage{
gr_diauxie(
  plate,
  n_baseline = 3,
  min_od = 0.05,
  window = 5,
  min_rate = 0.1,
  drop_frac = 0.5
)
}
\arguments{
\item{plate}{A \code{gr_plate} object.}

\item{n_baseline}{Number of initial readings averaged as the baseline that
is subtracted before fitting. Default \code{3}.}

\item{min_od}{For \code{method = "easylinear"}: baseline-subtracted values must
exceed this before a point enters the log-scale regressions. Default
\code{0.05}: generous relative to typical OD noise, because log-scale noise at
near-blank readings otherwise inflates the steepest-window estimate.
Lower it for low-density experiments with a quiet reader.}

\item{window}{For \code{method = "easylinear"}: number of consecutive points per
rolling regression. Default \code{5} (Hall et al.'s choice).}

\item{min_rate}{Minimum per-capita rate (in 1/time units) for a slope peak
to count as a growth phase. Default \code{0.1}.}

\item{drop_frac}{The slope must fall below this fraction of the smaller of
two neighbouring peaks for them to count as separate phases. Default
\code{0.5}.}
}
\value{
A tibble with one row per well: \code{n_phases}, \code{diauxic}
(\code{n_phases >= 2}; \code{NA} where the profile could not be estimated),
per-phase rates and times \code{r1}/\code{t1}, \code{r2}/\code{t2} (the two largest-rate
phases in time order; \code{NA} where absent), \code{trough_t} (time of the
minimum between them), and \code{note}.
}
\description{
Scans every well for multiple growth phases — the classic diauxic shift
when a culture exhausts one carbon source and switches to another. The
per-capita growth rate profile is estimated by rolling regressions on the
log-scale curve (the same machinery as \code{gr_fit(method = "easylinear")});
distinct phases appear as separate peaks in that profile with a genuine
trough between them.
}
\details{
A phase is a local maximum of the (median-smoothed) slope profile of at
least \code{min_rate}. Two neighbouring peaks count as separate phases only if
the slope drops below \code{drop_frac} times the smaller peak somewhere between
them — otherwise they are merged and the higher one kept. This guards
against calling noise wiggles "phases".
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
dx <- gr_diauxie(plate)
table(dx$diauxic, useNA = "ifany")
}
\seealso{
\code{\link[=gr_fit]{gr_fit()}} for single-phase rate estimation; use
\code{\link[=gr_plot_curves]{gr_plot_curves()}} on flagged wells to see the shift.
}
