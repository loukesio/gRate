% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{gr_layout}
\alias{gr_layout}
\title{Attach a plate layout (well metadata) to a \code{gr_plate}}
\usage{
gr_layout(
  plate,
  layout,
  name = "content",
  bio_rep = NULL,
  tech_rep = NULL,
  ...
)
}
\arguments{
\item{plate}{A \code{gr_plate} object.}

\item{layout}{A data frame or path to a CSV/TSV/Excel file, in long or grid
format.}

\item{name}{Name for the metadata column created from a grid layout
(default \code{"content"}). Ignored for long layouts.}

\item{bio_rep}{Optional name of the layout column identifying biological
replicates; it is renamed to \code{bio_rep}.}

\item{tech_rep}{Optional name of the layout column identifying technical
replicates; it is renamed to \code{tech_rep}.}

\item{...}{Additional arguments passed to the file reader when \code{layout} is
a path.}
}
\value{
The \code{gr_plate} with metadata columns added to \verb{$data}.
}
\description{
Joins experimental design information — strain, treatment, replicate,
anything per-well — onto the plate data. Two layout formats are accepted:
}
\details{
\itemize{
\item \strong{long} — a data frame or CSV with a \code{well} column plus one column per
metadata variable. This is the recommended format because it carries any
number of variables at once.
\item \strong{grid} — an 8 x 12 table mirroring the physical plate (optionally with
a leading column of row letters A-H), where each cell holds one value.
The values become a single metadata column named by \code{name}. Call
\code{gr_layout()} repeatedly to add several grid variables.
}

The format is detected automatically: a table with a \code{well} column is
treated as long, an 8-row table with 12 value columns as a grid.
\subsection{Biological and technical replicates}{

gRate treats two metadata columns as special: \code{bio_rep} (biological
replicate — independent cultures) and \code{tech_rep} (technical replicate —
the same culture measured in several wells). Layout columns already named
\code{bio_rep} / \code{tech_rep} are picked up automatically; otherwise point the
\code{bio_rep} / \code{tech_rep} arguments at the layout columns holding them and
they are renamed on the way in. Once designated, they show up in
\link[=new_gr_plate]{print()}, can colour \code{\link[=gr_plot_curves]{gr_plot_curves()}}, and technical
replicates can be averaged on export
(\code{gr_export(collapse_tech = TRUE)}).
}
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))

# long layout: well + any number of metadata columns
plate <- gr_layout(
  plate,
  system.file("extdata", "layout_long.csv", package = "gRate")
)
plate

# grid layout: an 8x12 table of values, one variable at a time
plate <- gr_layout(
  plate,
  system.file("extdata", "layout_grid.csv", package = "gRate"),
  name = "strain_grid"
)
head(plate$data)
}
\seealso{
\code{\link[=gr_read]{gr_read()}}, \code{\link[=gr_qc]{gr_qc()}}
}
