% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{as_growthcurver}
\alias{as_growthcurver}
\title{Convert a \code{gr_plate} to growthcurver input}
\usage{
as_growthcurver(plate, drop_flagged = FALSE)
}
\arguments{
\item{plate}{A \code{gr_plate} object.}

\item{drop_flagged}{If \code{TRUE}, wells flagged by \code{\link[=gr_qc]{gr_qc()}} are removed from
the export. Default \code{FALSE} — gRate flags, you decide. Requires
\code{\link[=gr_qc]{gr_qc()}} to have run.}
}
\value{
A wide data frame (\code{time} + one column per well).
}
\description{
Shorthand for \code{gr_export(plate, as = "growthcurver", ...)}: a wide data
frame with a \code{time} column and one column per well, the format
\code{growthcurver::SummarizeGrowthByPlate()} takes directly.
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
plate <- gr_qc(plate)
d <- as_growthcurver(plate, drop_flagged = TRUE)
\dontrun{
growthcurver::SummarizeGrowthByPlate(d)
}
}
