% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gr_plate.R
\name{gr_summarise}
\alias{gr_summarise}
\title{Per-well summary statistics}
\usage{
gr_summarise(plate, n_baseline = 3)
}
\arguments{
\item{plate}{A \code{gr_plate} object.}

\item{n_baseline}{Number of initial timepoints averaged to estimate the
baseline of each well.}
}
\value{
A tibble with one row per well and columns \code{well}, \code{row}, \code{col},
\code{baseline}, \code{max_od}, \code{delta_od} (max minus baseline), and \code{auc}.
}
\description{
Computes simple per-well summaries of the growth curves: baseline (mean of
the first \code{n_baseline} readings), maximum value, rise above baseline, and
area under the curve. Used internally by \code{\link[=gr_qc]{gr_qc()}}, \code{\link[=gr_spatial]{gr_spatial()}} and
\code{\link[=gr_plot_plate]{gr_plot_plate()}}, and handy on its own for a quick look at a plate.
}
\examples{
path <- system.file("extdata", "growth_long.csv", package = "gRate")
plate <- gr_read(path)
gr_summarise(plate)
}
