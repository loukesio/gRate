% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{gr_fit}
\alias{gr_fit}
\title{Fit growth models and estimate growth parameters per well}
\usage{
gr_fit(
  plate,
  method = c("logistic", "gompertz", "compare", "easylinear"),
  n_baseline = 3,
  min_od = 0.05,
  window = 5,
  min_r2 = 0.97,
  boot = 0,
  conf_level = 0.95
)
}
\arguments{
\item{plate}{A \code{gr_plate} object, ideally after \code{\link[=gr_qc]{gr_qc()}} (and optionally
\code{\link[=gr_spatial]{gr_spatial()}}).}

\item{method}{\code{"logistic"}, \code{"gompertz"}, \code{"compare"}, or \code{"easylinear"}
(see Details).}

\item{n_baseline}{Number of initial readings averaged as the baseline that
is subtracted before fitting. Default \code{3}.}

\item{min_od}{For \code{method = "easylinear"}: baseline-subtracted values must
exceed this before a point enters the log-scale regressions. Default
\code{0.05}: generous relative to typical OD noise, because log-scale noise at
near-blank readings otherwise inflates the steepest-window estimate.
Lower it for low-density experiments with a quiet reader.}

\item{window}{For \code{method = "easylinear"}: number of consecutive points per
rolling regression. Default \code{5} (Hall et al.'s choice).}

\item{min_r2}{For \code{method = "easylinear"}: minimum R-squared for a window
to be considered exponential growth. Default \code{0.97}.}

\item{boot}{Number of bootstrap resamples for confidence intervals on the
parameters. Default \code{0} (off). With \code{boot > 0} (200 is a reasonable
choice), residuals are resampled and the model refitted per resample;
\verb{$fit} gains percentile intervals \code{r_lo}/\code{r_hi} (both methods) and
\code{K_lo}/\code{K_hi} (logistic only — easylinear's K is an empirical maximum,
not a fitted parameter). Wells where fewer than half the resamples
converge get \code{NA} intervals.}

\item{conf_level}{Confidence level for the bootstrap intervals. Default
\code{0.95}.}
}
\value{
The \code{gr_plate} with \verb{$fit} set: a tibble with one row per well and
columns \code{well}, \code{row}, \code{col}, \code{r}, \code{K}, \code{N0}, \code{lag}, \code{t_rmax},
\code{doubling_time}, \code{sigma} (residual SD on the value scale), \code{fit_ok}, and
\code{note}. \code{plate$data} gains a \code{fitted} column.
}
\description{
Estimates growth parameters for every well and stores them in \code{plate$fit}.
Two methods are available:
}
\details{
\itemize{
\item \code{"logistic"} (default) — a parametric fit of the logistic growth model
via \code{\link[stats:nls]{stats::nls()}} with the self-starting \code{\link[stats:SSlogis]{stats::SSlogis()}} model, on
baseline-subtracted values. Returns the carrying capacity \code{K}, the
intrinsic growth rate \code{r}, the initial density \code{N0}, the time of maximum
growth \code{t_rmax} (the inflection point), the lag time (tangent at the
inflection extrapolated to the baseline), and the doubling time
\code{ln(2)/r}.
\item \code{"gompertz"} — a parametric fit of the Gompertz model via
\code{\link[stats:SSgompertz]{stats::SSgompertz()}}. Growth curves with a long deceleration phase are
often Gompertz-shaped rather than logistic. Here \code{r} is the Gompertz
rate constant \emph{k} — a different scale from the logistic \code{r}, so compare
strains within one method, and compare models with \code{"compare"}.
\item \code{"compare"} — fits \strong{both} logistic and Gompertz to every well and
keeps the model with the lower AIC. \verb{$fit} gains \code{model} (the winner),
\code{aic_logistic}, \code{aic_gompertz}, and \code{delta_aic} (the winner's margin;
small values mean the data cannot really tell the models apart).
\item \code{"easylinear"} — a nonparametric estimate after Hall et al. (2014):
rolling linear regressions of \code{log(value - baseline)} over \code{window}
consecutive points, keeping only windows whose R-squared exceeds
\code{min_r2}; \code{r} is the steepest remaining slope — the maximum per-capita
growth rate, often written \eqn{\mu_{max}}. The R-squared filter is what
makes this robust: windows dominated by low-OD noise are simply
rejected. \code{K} is the (spike-resistant, running-median smoothed) maximum
of the curve, and the lag time is where the fitted exponential crosses
the initial log-density. Use this when curves are not logistic
(diauxie, weird shapes).
}

Wells where the fit fails — dead wells have nothing to fit — get
\code{fit_ok = FALSE} and an explanatory \code{note}, never an error. Flagged wells
are fitted like any others (flags never delete); check \code{plate$qc} before
trusting their parameters. If the plate was corrected with \code{\link[=gr_spatial]{gr_spatial()}},
the corrected values are fitted.

Fitted curves are stored in a \code{fitted} column of \code{plate$data} (\code{NA} where
the model makes no prediction), so \code{\link[=gr_plot_fit]{gr_plot_fit()}} can overlay them on the
data. Refitting (e.g. with the other method) simply replaces the previous
results.
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
plate <- gr_qc(plate)
plate <- gr_fit(plate)
plate
head(plate$fit)
gr_plot_plate(plate, "r")
}
\references{
Hall BG, Acar H, Nandipati A, Barlow M (2014). Growth rates
made easy. \emph{Molecular Biology and Evolution} 31(1), 232-238.
}
\seealso{
\code{\link[=gr_plot_fit]{gr_plot_fit()}} to inspect fits, \code{\link[=gr_plot_plate]{gr_plot_plate()}} to map any fit
parameter, \code{\link[=as_growthcurver]{as_growthcurver()}}/\code{\link[=as_gcplyr]{as_gcplyr()}} if you prefer those fitting
engines.
}
