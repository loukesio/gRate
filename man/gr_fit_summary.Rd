% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{gr_fit_summary}
\alias{gr_fit_summary}
\title{Summarise growth parameters across replicates}
\usage{
gr_fit_summary(plate, by = NULL, drop_flagged = TRUE)
}
\arguments{
\item{plate}{A \code{gr_plate} object after \code{\link[=gr_fit]{gr_fit()}}.}

\item{by}{Character vector of metadata columns to group by. Default:
all metadata columns except \code{bio_rep} and \code{tech_rep}. With no metadata at
all, a single overall summary row is returned.}

\item{drop_flagged}{If \code{TRUE} (default), wells flagged by \code{\link[=gr_qc]{gr_qc()}} are
excluded before averaging. Failed fits are always excluded.}
}
\value{
A tibble with the grouping columns, \code{n_wells}, and \verb{<param>_mean} /
\verb{<param>_sd} for \code{r}, \code{K}, \code{lag}, and \code{doubling_time}.
}
\description{
Aggregates the per-well growth parameters from \code{\link[=gr_fit]{gr_fit()}} over experimental
conditions: mean and standard deviation of \code{r}, \code{K}, \code{lag}, and
\code{doubling_time}, with the number of wells behind each estimate. This is the
step most fitting packages leave to you — combining technical and
biological replicates \emph{after} excluding wells that QC flagged or whose fit
failed.
}
\details{
By default wells are grouped by every metadata column added with
\code{\link[=gr_layout]{gr_layout()}} except the replicate identifiers (\code{bio_rep}, \code{tech_rep}), so
replicates of the same condition are averaged together. Pass \code{by} to group
differently — e.g. \code{by = c("strain", "medium", "bio_rep")} keeps biological
replicates separate and averages only technical ones.
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate")) |>
  gr_layout(system.file("extdata", "layout_long.csv", package = "gRate")) |>
  gr_qc() |>
  gr_fit()
gr_fit_summary(plate)
gr_fit_summary(plate, by = c("strain", "medium", "bio_rep"))
}
