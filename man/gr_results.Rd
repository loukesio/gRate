% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{gr_results}
\alias{gr_results}
\title{The results table: growth parameters joined with your metadata}
\usage{
gr_results(
  plate,
  params = c("r", "K", "lag", "doubling_time"),
  drop_flagged = FALSE,
  file = NULL
)
}
\arguments{
\item{plate}{A \code{gr_plate} object after \code{\link[=gr_fit]{gr_fit()}}.}

\item{params}{Which fit parameters to include, any of \code{"r"}, \code{"K"},
\code{"lag"}, \code{"doubling_time"}, \code{"N0"}, \code{"t_rmax"}, \code{"sigma"}. Defaults to
the first four — the ones most analyses care about.}

\item{drop_flagged}{If \code{TRUE}, wells flagged by \code{\link[=gr_qc]{gr_qc()}} are removed.
Default \code{FALSE} — the \code{flagged} and \code{reasons} columns are included
instead, so nothing disappears silently.}

\item{file}{Optional path; if given, the table is also written there as a
CSV and the tibble returned invisibly.}
}
\value{
A tibble with columns \code{well}, \code{row}, \code{col}, the metadata columns,
the requested parameters, \code{fit_ok}, \code{note}, and (when QC has run)
\code{flagged} and \code{reasons}.
}
\description{
Returns the table you actually want after a fit: one row per well with the
experimental metadata from \code{\link[=gr_layout]{gr_layout()}} (strain, medium, replicates, ...),
the growth parameters from \code{\link[=gr_fit]{gr_fit()}}, and the QC verdict from \code{\link[=gr_qc]{gr_qc()}} —
ready for plotting, statistics, or saving.
}
\details{
This differs from \code{plate$fit} (parameters only, no metadata) and from
\code{\link[=gr_fit_summary]{gr_fit_summary()}} (already averaged over replicates): \code{gr_results()} is
the full per-well table that sits between the two.
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate")) |>
  gr_layout(system.file("extdata", "layout_long.csv", package = "gRate")) |>
  gr_qc() |>
  gr_fit()

gr_results(plate)
gr_results(plate, params = c("r", "K"), drop_flagged = TRUE)
\dontrun{
gr_results(plate, file = "run1_growth_parameters.csv")
}
}
\seealso{
\code{\link[=gr_fit]{gr_fit()}}, \code{\link[=gr_fit_summary]{gr_fit_summary()}}
}
