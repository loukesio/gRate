% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{gr_plot_curves}
\alias{gr_plot_curves}
\title{Faceted growth curves with flagged wells highlighted}
\usage{
gr_plot_curves(plate, colour_by = NULL, wells = NULL, raw = FALSE)
}
\arguments{
\item{plate}{A \code{gr_plate} object.}

\item{colour_by}{Optional metadata column (added by \code{\link[=gr_layout]{gr_layout()}}) to
colour curves by instead of QC status.}

\item{wells}{Optional character vector of well ids to restrict the plot to
(e.g. \code{c("A1", "B3")}); facets then wrap instead of using the plate grid.}

\item{raw}{If \code{TRUE} and the plate was spatially corrected, plot the
uncorrected \code{value_raw} instead. Default \code{FALSE}.}
}
\value{
A ggplot object.
}
\description{
Plots every well's curve in its plate position (8 x 12 facet grid). Wells
flagged by \code{\link[=gr_qc]{gr_qc()}} are drawn in colour with the flag reasons available in
the returned data; unflagged wells are grey.
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
plate <- gr_qc(plate)
gr_plot_curves(plate)
gr_plot_curves(plate, wells = c("A1", "C5", "F8"))
}
