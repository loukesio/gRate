% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{gr_plot_fit}
\alias{gr_plot_fit}
\title{Growth curves with fitted models overlaid}
\usage{
gr_plot_fit(plate, wells = NULL)
}
\arguments{
\item{plate}{A \code{gr_plate} object after \code{\link[=gr_fit]{gr_fit()}}.}

\item{wells}{Optional character vector of well ids to restrict the plot to;
facets then wrap instead of using the plate grid.}
}
\value{
A ggplot object.
}
\description{
Plots the observed readings as points and the curve fitted by \code{\link[=gr_fit]{gr_fit()}}
as a line, one facet per well in plate position (or wrapped, for a
subset). For \code{method = "logistic"} the line spans the whole run; for
\code{method = "easylinear"} it is the fitted exponential drawn over the
winning regression window. Wells whose fit failed show points only.
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
plate <- gr_fit(gr_qc(plate))
gr_plot_fit(plate, wells = c("A1", "B3", "C5"))
}
