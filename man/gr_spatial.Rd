% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{gr_spatial}
\alias{gr_spatial}
\title{Estimate and correct spatial (positional) artifacts}
\usage{
gr_spatial(
  plate,
  stat = c("max_od", "auc"),
  correct = TRUE,
  exclude_flagged = TRUE,
  n_baseline = 3
)
}
\arguments{
\item{plate}{A \code{gr_plate} object, ideally after \code{\link[=gr_qc]{gr_qc()}}.}

\item{stat}{Summary statistic to estimate effects on: \code{"max_od"} (default)
or \code{"auc"}.}

\item{correct}{If \code{TRUE} (default), divide each curve by its estimated bias
factor. The uncorrected values are kept in a \code{value_raw} column. If
\code{FALSE}, effects are estimated and stored but the data are untouched.}

\item{exclude_flagged}{If \code{TRUE} (default) and QC has run, flagged wells are
excluded from effect estimation.}

\item{n_baseline}{Passed to \code{\link[=gr_summarise]{gr_summarise()}} for the statistic computation.}
}
\value{
The \code{gr_plate} with a \verb{$spatial} element: a list with the \code{stat}
used, named vectors \code{row_effects} and \code{col_effects} (multiplicative, 1 =
no bias), the \code{overall} level, a per-well tibble \code{factors}, and
\code{corrected} (logical). If \code{correct = TRUE}, \verb{$data$value} is replaced by
the corrected values and the original kept as \verb{$data$value_raw}.
}
\description{
\strong{Experimental.} Wells at the plate edge
often read systematically lower (evaporation, temperature gradients). This
function estimates row and column effects on a per-well summary statistic
with Tukey's median polish (\code{\link[stats:medpolish]{stats::medpolish()}}) and, optionally, corrects
every curve multiplicatively.
}
\details{
The statistic is computed per well, log-transformed, and decomposed into
overall + row + column effects. The multiplicative bias factor for a well
is \code{exp(row_effect + col_effect)}; correction divides the whole curve by
that factor. Wells flagged by \code{\link[=gr_qc]{gr_qc()}} are excluded from \emph{estimating} the
effects (they would distort the medians) but still receive the correction.

Median polish captures row and column trends. A pure "outer ring" effect is
mostly absorbed (rows A/H and columns 1/12), but corner wells are corrected
twice over — inspect \code{gr_plot_plate(plate, "max_od")} before and after, and
treat this correction as a diagnostic aid, not gospel. It is deliberately
simple (no mixed models): if your design confounds treatments with plate
position, no correction can rescue it — randomise the layout instead.
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
plate <- gr_qc(plate)
plate <- gr_spatial(plate)
plate$spatial$row_effects
plate
}
\seealso{
\code{\link[=gr_plot_plate]{gr_plot_plate()}} to inspect the spatial pattern.
}
