% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{gr_lag}
\alias{gr_lag}
\title{Lag time by several methods, with agreement}
\usage{
gr_lag(
  plate,
  methods = c("logistic", "gompertz", "easylinear", "threshold"),
  n_baseline = 3,
  min_od = 0.05,
  window = 5,
  min_r2 = 0.97,
  threshold = 0.05,
  max_sd = 1.5
)
}
\arguments{
\item{plate}{A \code{gr_plate} object.}

\item{methods}{Which lag definitions to compute. Default: all four.}

\item{n_baseline}{Number of initial readings averaged as the baseline that
is subtracted before fitting. Default \code{3}.}

\item{min_od}{For \code{method = "easylinear"}: baseline-subtracted values must
exceed this before a point enters the log-scale regressions. Default
\code{0.05}: generous relative to typical OD noise, because log-scale noise at
near-blank readings otherwise inflates the steepest-window estimate.
Lower it for low-density experiments with a quiet reader.}

\item{window}{For \code{method = "easylinear"}: number of consecutive points per
rolling regression. Default \code{5} (Hall et al.'s choice).}

\item{min_r2}{For \code{method = "easylinear"}: minimum R-squared for a window
to be considered exponential growth. Default \code{0.97}.}

\item{threshold}{Rise above baseline (in OD units) defining the
\code{threshold} lag. Default \code{0.05}.}

\item{max_sd}{Maximum standard deviation across methods (in time units)
for a well to count as \code{agree = TRUE}. Default \code{1.5}.}
}
\value{
A tibble with one row per well: \verb{lag_<method>} columns, \code{lag_mean},
\code{lag_sd}, \code{lag_range} (max minus min), \code{n_methods} (how many succeeded),
and \code{agree} (\code{NA} when fewer than two methods succeeded).
}
\description{
Lag time is notoriously method-dependent: tangent constructions, model
fits, and threshold crossings can disagree by hours on the same well.
Rather than pretending one definition is "the" lag, \code{gr_lag()} computes it
several ways per well and reports how much the methods agree — large
disagreement usually means the curve shape violates one method's
assumptions, and is worth a look with \code{\link[=gr_plot_curves]{gr_plot_curves()}}.
}
\details{
Methods:
\itemize{
\item \code{logistic} — tangent at the inflection of a logistic fit, extrapolated
to the baseline (as in \code{\link[=gr_fit]{gr_fit()}}).
\item \code{gompertz} — the same tangent construction on a Gompertz fit.
\item \code{easylinear} — the fitted exponential of the steepest log-linear window
extrapolated down to the initial log-density (Hall et al. 2014).
\item \code{threshold} — the first time the (lightly smoothed) curve rises more
than \code{threshold} above baseline. Model-free and simple; by construction
it reads later than tangent methods on shallow curves and earlier on
steep ones.
}

Methods that fail on a well (e.g. anything model-based on a dead well)
contribute \code{NA} and are excluded from the agreement statistics.
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
lags <- gr_lag(plate)
head(lags)
subset(lags, !agree)  # wells where the definitions disagree
}
\seealso{
\code{\link[=gr_fit]{gr_fit()}} for the tangent definitions.
}
