% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_wells.R
\name{gr_qc}
\alias{gr_qc}
\title{Flag problematic wells}
\usage{
gr_qc(
  plate,
  checks = c("no_growth", "spike", "drift", "late_jump", "noisy"),
  no_growth_delta = 0.05,
  n_baseline = 3,
  spike_delta = 0.1,
  drift_r2 = 0.95,
  late_frac = 0.6,
  late_jump_delta = 0.1,
  noisy_sd = 0.02,
  noisy_span = 0.25
)
}
\arguments{
\item{plate}{A \code{gr_plate} object.}

\item{checks}{Character vector of checks to run. Defaults to all of
\code{"no_growth"}, \code{"spike"}, \code{"drift"}, \code{"late_jump"}, \code{"noisy"}.}

\item{no_growth_delta}{Minimum rise above baseline (in OD units) for a well
to count as grown. Default \code{0.05}.}

\item{n_baseline}{Number of initial readings averaged as the baseline.
Default \code{3}.}

\item{spike_delta}{Minimum deviation from both neighbouring readings for a
point to count as a spike. Default \code{0.1}.}

\item{drift_r2}{Squared correlation with time above which a growing,
monotone curve is considered linear drift. Default \code{0.95}.}

\item{late_frac}{Fraction of the run that must be flat before a rise counts
as a late jump. Default \code{0.6}.}

\item{late_jump_delta}{Minimum rise after the flat period to flag a late
jump. Default \code{0.1}.}

\item{noisy_sd}{Maximum robust residual spread (MAD) around a loess smooth.
Default \code{0.02}.}

\item{noisy_span}{\code{span} passed to \code{\link[stats:loess]{stats::loess()}} for the noise check.
Default \code{0.25} — small enough that a sigmoid is followed closely, so the
residuals measure noise rather than lack of fit.}
}
\value{
The \code{gr_plate} with \verb{$qc} set: a tibble with one row per well,
one logical column per check, \code{flagged} (any check failed) and \code{reasons}
(comma-separated names of failed checks). The thresholds used are stored
in \code{attr(plate$qc, "thresholds")}.
}
\description{
Runs per-well quality checks on the growth curves and records the results
in \code{plate$qc}. Wells are \strong{flagged, never deleted} — downstream you decide
what to drop (see \code{drop_flagged} in \code{\link[=gr_export]{gr_export()}}).
}
\details{
Available checks:
\itemize{
\item \code{no_growth} — the curve never rises more than \code{no_growth_delta} above its
baseline (mean of the first \code{n_baseline} readings). Empty or dead wells.
\item \code{spike} — one or more single-timepoint jumps: a reading that is more than
\code{spike_delta} above \emph{both} neighbours (or below both). Typical of
condensation, bubbles, or read glitches; a genuine growth rise never
reverses in a single step.
\item \code{drift} — the well rises but the curve is essentially a straight line
(squared correlation with time above \code{drift_r2}, positive slope) with no
sigmoid shape. Typical of evaporation or baseline drift.
\item \code{late_jump} — flat for the first \code{late_frac} of the run, then a sudden
rise of more than \code{late_jump_delta}. Contamination-like.
\item \code{noisy} — the robust spread (median absolute deviation) of the residuals
around a loess smooth exceeds \code{noisy_sd}. Robust so that isolated spikes,
which have their own flag, do not also register as noise.
}

Every threshold is an argument; the defaults are sensible for OD600 growth
curves read every 5–30 minutes but should be tuned to your instrument.
}
\examples{
plate <- gr_read(system.file("extdata", "growth_long.csv", package = "gRate"))
plate <- gr_qc(plate)
plate
subset(plate$qc, flagged)
}
\seealso{
\code{\link[=gr_plot_plate]{gr_plot_plate()}} to visualise flags, \code{\link[=gr_export]{gr_export()}} to drop
flagged wells on export.
}
